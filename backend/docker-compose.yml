version: '3.8'

services:
  backend:
    build: .
    container_name: aegisiq_backend
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - DEMO_MODE=${DEMO_MODE:-True}
    restart: always